{
  "name": "digital-golf",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "spacetime build",
    "publish": "spacetime publish",
    "check-courses": "esbuild scripts/course-check.ts --bundle --platform=node --format=esm --log-level=error --outfile=.course-check.mjs && node .course-check.mjs"
  },
  "license": "ISC",
  "dependencies": {
    "spacetimedb": "^2.8.3"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "~5.6.2"
  }
}
